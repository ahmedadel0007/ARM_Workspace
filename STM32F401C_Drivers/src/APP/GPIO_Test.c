#include "STD_TYPES.h"

#include "RCC_interface.h"

#include "GPIO.h"

/*
int main()
{

	RCC_ControlClk(HSE,ON);

		Rcc_SelectsystemClk(RCC_HSE);


		RCC_enuEnablePreipheral(AHB1_BUS,GPIOA_RCC);

	RCC_enuEnablePreipheral(AHB1_BUS,GPIOA_RCC);

			GPIO_Pinconfig_t led;

			//u8 Local_var=0;

			led.GPIO_portnumber=GPIO_A;
			led.GPIO_pinnumber=GPIO_PIN0;
			led.GPIO_MODE=GPIO_MODE_OUTPUT_PP;
			led.GPIO_SPEED = GPIO_SPEED_LOW;



			Gpio_enuInit (&led);

			GPIO_SetPin_value(GPIO_A,GPIO_PIN0,GPIO_STATE_HIGH);

			//GPIO_GetPin_value (GPIO_A,GPIO_PIN0,&Local_var);






	while(1)
	{

	}
	return 0;
}
*/